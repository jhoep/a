[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["npm install --omit=dev", "python3.11 -m pip install -U yt-dlp --break-system-packages"]

[start]
cmd = "node --dns-result-order=ipv4first index.js"
