[phases.setup]
nixPkgs = ["nodejs_20", "python3", "python3Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["npm install --omit=dev", "pip3 install -U yt-dlp"]

[start]
cmd = "node --dns-result-order=ipv4first index.js"
