[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python311",
  "python311Packages.pip",
  "ffmpeg",
  "yt-dlp"
]

[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node --dns-result-order=ipv4first index.js"